# Copy to .env and fill in (optional - API works without keys, just less reliable live fetch)
JINA_API_KEY=
# Required to enable /refresh (fail-closed without it). Send as Authorization: Bearer <key>.
ADMIN_KEY=
